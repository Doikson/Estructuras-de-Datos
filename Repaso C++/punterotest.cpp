#include <iostream>

int main() {
    int a = 42; // declaramos la variable 'a' y le asignamos el valor 42
    std::cout << "La direccion de memoria de 'a' es: " << &a << std::endl; // imprimimos la direccion de memoria de 'a'
    return 0;
}

